# Gitee Ai - JMeter AI Plugin Configuration

##### CHAT SETTINGS #####

### Gitee Ai supports Anthropic,OpenAI, Ollama APIs and Chinese LLM providers (DeepSeek, Zhipu, Moonshot, MiniMax, LangCat)

# =====================================================
# GLOBAL LLM DEFAULTS
# =====================================================
# These defaults apply to ALL providers unless overridden per-provider.
# Per-provider overrides take precedence (e.g., deepseek.temperature=0.3).

# Global temperature (default: 0.7)
# Lower values = more deterministic, higher values = more creative
jmeter.ai.temperature=0.7

# Global max tokens per response (default: 4096)
jmeter.ai.max.tokens=4096

# Global max conversation history size for Agent Loop (default: 120)
# Controls the maximum number of messages replayed from session history per agent turn.
# Acts as a hard cap complementary to jmeter.ai.context.window.tokens (token-level trimming).
# Aligned with Nanobot's max_messages design (port from session.get_history).
# Set to 0 to disable message-count trimming (use token budget only).
jmeter.ai.max.history.size=120

# Global reasoning effort: none, minimal, low, medium, high, xhigh, max (default: medium)
# - none: no reasoning/thinking (faster, cheaper)
# - minimal: bare-minimum reasoning
# - low: light reasoning
# - medium: balanced reasoning
# - high: deep reasoning (slower, uses more tokens)
# - xhigh: very deep reasoning (slower, more tokens)
# - max: absolute maximum reasoning (highest token cost; requires a MAX-capable model,
#         e.g. Kimi K3 or GPT-5.6-class)
# Maps to: Claude extended thinking budget, OpenAI reasoning_effort, Ollama ThinkMode
# Note: minimal/xhigh/max only take effect on providers whose API supports that level;
# values are passed through to the provider as-is (unknown values map to high).
jmeter.ai.reasoning.effort=medium

# Global default model (default: deepseek-v4-flash)
# All providers use this model unless explicitly overridden at runtime
jmeter.ai.default.model=deepseek-v4-flash

# Global default provider (default: deepseek)
# Determines which provider to use for API calls
# Options: anthropic, openai, ollama, deepseek, zhipu, moonshot, minimax, langcat
jmeter.ai.default.provider=deepseek

# Global context window size in tokens (default: 65536)
# Used by: ContextWindowManager, MemoryConsolidator, AgentRunner
jmeter.ai.context.window.tokens=65536

# Global max tool iterations per agent loop run (default: 50)
jmeter.ai.max.tool.iterations=50
# =====================================================

# =====================================================
# UNIFIED SYSTEM PROMPT (Applies to all AI providers)
# =====================================================
# This system prompt is used by ALL AI providers (Claude, OpenAI, DeepSeek, Zhipu, Moonshot, MiniMax, LangCat, Ollama)
# Customize this to change how the AI assistant responds to your queries
#
# To use a custom prompt, uncomment and edit the lines below:
#jmeter.ai.system.prompt=You are a JMeter expert assistant. Help users with JMeter test plans.
# =====================================================


### Gitee Ai supports Anthropic,OpenAI, Ollama APIs and Chinese LLM providers (DeepSeek, Zhipu, Moonshot, MiniMax, LangCat)

# Ollama Configuration
# Ollama base URL (OpenAI-compatible endpoint). Must include the /v1 suffix.
ollama.api.base.url=http://localhost:11434/v1

# Anthropic API Base URL (optional)
# Default: https://api.anthropic.com
# Uncomment to use a custom endpoint (e.g., for proxy or regional deployment)
#anthropic.api.base.url=https://api.anthropic.com

# Anthropic API Key (required)
anthropic.api.key=YOUR_API_KEY

# Anthropic API Logging Level
# Set to "info" for basic request/response logging or "debug" for verbose logging
# Leave empty to disable logging
anthropic.log.level=

# OpenAI API Base URL (optional)
# Default: https://api.openai.com
# Uncomment to use a custom endpoint (e.g., for proxy or Azure OpenAI)
#openai.api.base.url=https://api.openai.com

# OpenAI API Key (required)
openai.api.key=YOUR_API_KEY
openai.log.level=

##### CHINESE LLM PROVIDER CONFIGURATION #####

# =====================================================
# Gitee Ai now supports Chinese LLM providers
# Supported providers: DeepSeek, Zhipu AI (GLM), Moonshot (Kimi), MiniMax, LangCat
# =====================================================

# -----------------------------------------------------
# DeepSeek Configuration
# -----------------------------------------------------
# API Key (required) - Get your key from https://platform.deepseek.com
deepseek.api.key=

# API Base URL (optional)
# Default: https://api.deepseek.com
# Uncomment to use a custom endpoint
deepseek.api.base.url=https://api.deepseek.com

# -----------------------------------------------------
# Zhipu AI (GLM) Configuration
# -----------------------------------------------------
# API Key (required) - Get your key from https://open.bigmodel.cn
zhipu.api.key=

# API Base URL (optional)
# Default: https://open.bigmodel.cn/api/paas/v4
zhipu.api.base.url=https://open.bigmodel.cn/api/paas/v4/

# -----------------------------------------------------
# Moonshot (Kimi) Configuration
# -----------------------------------------------------
# API Key (required) - Get your key from https://platform.moonshot.cn
moonshot.api.key=

# API Base URL (optional)
# Default: https://api.moonshot.cn/v1 (China endpoint; matches the official Kimi K3 quickstart).
# The international endpoint is https://api.moonshot.ai/v1 — note it uses a separate
# account / API keys, so set this to match where your key was issued.
moonshot.api.base.url=https://api.moonshot.cn/v1

# -----------------------------------------------------
# MiniMax Configuration
# -----------------------------------------------------
# API Key (required) - Get your key from https://api.minimax.com
minimax.api.key=

# API Base URL (optional)
# Default: https://api.minimax.io/v1
minimax.api.base.url=https://api.minimaxi.com/v1

# -----------------------------------------------------
# LangCat Configuration
# -----------------------------------------------------
# API Key (required) - Get your key from https://longcat.chat
langcat.api.key=

# API Base URL (optional)
# Default: https://api.longcat.chat/openai/v1
# Uncomment to use a custom endpoint
langcat.api.base.url=https://api.longcat.chat/openai/v1

##### AGENT LOOP CONFIGURATION #####

# Enable/disable the Agent Loop architecture
# When enabled, provides tool calling, memory management, and session persistence
agent.enabled=true

# Agent Loop Iteration Settings
# Note: max.tool.iterations and context.window.tokens are now configured in GLOBAL LLM DEFAULTS above

# Tool Result Truncation
# Maximum characters for tool results before truncation (default:16000)
agent.tool.result.max.chars=16000

# Injection Queue Settings
# Maximum queued injection messages per session (default: 20)
jmeter.ai.injection.queue.size=20

# Maximum injection messages processed per agent turn (default: 3)
jmeter.ai.injection.max.per.turn=3


##### WORKSPACE CONFIGURATION #####

# Workspace Path for Agent Loop
# Location for MEMORY.md, HISTORY.md, session files, skills, and bootstrap templates
# This workspace is used by: MemoryStore, SessionManager, SkillsLoader, ContextBuilder
# Default: {jmeter.home}/bin/jmeter-agent
# Override only if you want a different location (absolute path recommended):
# agent.workspace.path=D:/some/path

##### MEMORY CONFIGURATION #####

# Enable/disable memory system
# When enabled, stores long-term memory and conversation history
agent.memory.enabled=true

# Close-time memory consolidation: when JMeter closes, this instance's unconsolidated
# messages are always archived to the shared HISTORY.md, and an interactive dialog asks
# whether to deep-distill them into long-term MEMORY.md.
# - "Yes" distills (writes MEMORY.md, which other instances' system prompts can see) and
#   shows progress before exit; "No" skips the LLM step (HISTORY.md archive still happens).
# This consolidation is always on (not configurable).

# Bounded timeout (ms) for the close-time deep distillation (the "Yes" path, writes
# MEMORY.md). On timeout/failure the already-archived HISTORY.md is kept and JMeter
# proceeds to exit. Default: 120000 (2 min).
agent.memory.consolidate-on-exit.timeout.ms=120000


##### SESSION CONFIGURATION #####

# Per-instance session files: each JMeter start uses its own session file keyed by a
# per-start instanceId ({pid}-{startedAtMs}), so multiple JMeter instances running at once
# no longer cross-contaminate a single shared session. Default true.
# Set false to revert to the legacy global "jmeter-ai-chat" session key (back-compat).
agent.session.per-instance=true

# Stale orphan-session reap TTL (days). At each start, sessions/ is scanned and
# {pid}-{startedAtMs}.jsonl files whose owning instance is confirmed dead (no live port file
# via PID+TCP double-check) AND whose last-modified exceeds this TTL are deleted.
# Active instances' files are never touched. Default 7.
agent.session.reap.ttl.days=7


##### TOOL CONFIGURATION #####

# JMeter Tool Output Truncation
# Maximum characters of a single string property value before truncation
# when serializing JMeter elements (e.g., JSR223 script, HTTP body data, long URLs).
# Applies to: get_test_plan_tree, get_selected_element, query_element_properties,
# find_element, parse_jmx_file.
# Truncated values are suffixed with "...(truncated)".
# Set to a very large value (e.g., 100000000) to effectively disable truncation.
jmeter.ai.tool.max.string.length=2048

# JMeter Tools
# Enable/disable JMeter-specific tools (get_element, optimize_element, etc.)
agent.tools.jmeter.enabled=true

# File System Tools
# Enable/disable file system tools (read_file, write_file, etc.)
# WARNING: Use with caution - may pose security risks
agent.tools.filesystem.enabled=true

# Allowed directories for file system tools (comma-separated)
# If empty, defaults to user home and current working directory
# Example: C:\\Projects,D:\\Work
#agent.tools.filesystem.allowed.dirs=

# Denied directories/files for file system tools (comma-separated)
# Paths listed here will be denied even if within allowed directories.
# Supports both directories (denies everything under) and individual files.
# Example: D:\\tmp\\secrets,D:\\tmp\\config\\private.key
#agent.tools.filesystem.denied.dirs=

# Web Search Tools
# Enable/disable web search tools (web_search, web_fetch)
# Requires API keys for search services
agent.tools.websearch.enabled=true

# Web Search Provider (default: jina)
# Options: brave, tavily, jina
agent.tools.websearch.provider=jina

# Maximum search results (default: 10)
agent.tools.websearch.max.results=10

# Search timeout in seconds (default: 30)
agent.tools.websearch.timeout=30

# Web Fetch Settings
# Fetch timeout in seconds (default: 30)
agent.tools.webfetch.timeout=30

# SSRF Protection
# Enable/disable SSRF (Server-Side Request Forgery) protection
# When enabled, blocks access to private/local networks
agent.tools.web.ssrf.protection=true

# Maximum redirects to follow (default: 5)
agent.tools.web.max.redirects=5

# Tool timeout in milliseconds (default: 30000)
agent.tools.timeout.ms=30000

# Exec Tool
# Enable/disable command execution tool (default: true)
# WARNING: Use with caution - allows shell command execution on the host system
agent.tools.exec.enabled=true

# Default timeout in seconds for command execution (default: 60, max: 600)
agent.tools.exec.timeout=60

# Default working directory for commands (optional)
# If set, restricts command execution to this directory and its subdirectories
#agent.tools.exec.working.dir=

# Deny patterns for dangerous commands (comma-separated regex patterns)
# If empty, uses built-in defaults (rm -rf, del /f, format, mkfs, shutdown, etc.)
#agent.tools.exec.deny.patterns=

# Additional directories to append to PATH (optional)
#agent.tools.exec.path.append=


##### SUBAGENT CONFIGURATION #####

# Async Subagent: the main agent can delegate a self-contained,
# read-only analysis task to a background subagent via the `spawn` tool. The
# subagent runs on a dedicated thread pool with an isolated toolset (read-only
# JMeter/file/web tools only — it cannot modify the test plan, run tests, write
# files, or spawn further subagents) and an ephemeral session that never touches
# the main conversation's history. Its result is folded back into the SAME
# conversation turn.

# Master switch (default: false). When disabled, the spawn and subagent_status
# tools are not registered and the main agent cannot spawn subagents.
agent.subagent.enabled=false

# Max concurrent subagents per main session (default: 1).
# A higher value lets the main agent run several subagents in parallel; at 1 they
# run serially. The main turn blocks at injection checkpoints until each finishes
# (bounded by the drain timeout below).
agent.subagent.max.concurrent=1

# Max tool iterations for a single subagent run (default: 50).
agent.subagent.max.iterations=50

# How long (seconds) the main turn parks waiting for a subagent result before
# giving up and continuing without it (default: 120, hard cap 300). A timed-out
# subagent keeps running; its result stays queryable via subagent_status and is
# delivered into a later turn if possible. Past this, a hung subagent is presumed
# dead so the turn is not held forever.
agent.subagent.drain.timeout.seconds=120

# How long (seconds) a finished subagent's status stays queryable via subagent_status
# (default: 60). A late/undeliverable result is kept this long so the main agent can
# still pull it, then reclaimed — bounds memory on a long-lived instance.
# 0 disables age-based reclaiming (never reclaim by TTL).
agent.subagent.status.retention.seconds=60

# Max number of finished (terminal) subagent statuses retained per session (default: 10).
# Oldest finished entries are evicted beyond this. 0 disables count-based eviction.
agent.subagent.status.max.completed=10


##### RUN RESULT CAPTURE CONFIGURATION #####

# Capture results of user-initiated (GUI Run button) test runs, not just runs the
# Agent starts via the run_test tool. A global JMeter Start.class pre-action listener
# injects the result collector before any local GUI run (Run / Run Thread Group), so
# get_test_status / get_test_results return live data for tests the user started too.
# Run provenance (USER vs AGENT) is recorded and shown in get_test_status.

# Master switch (default: true). Only gates the Start.class pre-listener registration:
# - When true: before a GUI-initiated run (Run / Run Thread Group), a listener component
#   named "__agent_result_collector__" (an AgentResultCollector SampleListener) is injected
#   under the TestPlan node of the GUI tree, so the run's samples are captured. It is
#   stripped before any save and auto-removed when the test ends.
# - When false: GUI Run-button runs are NOT captured (no listener is injected), but run_test
#   still captures (it injects its own collector regardless of this toggle).
# - The Save.class strip-listener (prevents the collector node from leaking into .jmx
#   files) is ALWAYS registered, independent of this toggle.
agent.runcapture.enabled=true


##### CHAT UI CONFIGURATION #####

# Chat Area Font Size (default: 0 = auto, uses system font size)
# Set to a positive integer to override the font size in points.
# Examples: 14 (compact), 16 (comfortable), 18 (large), 22 (very large)
#ai.chat.font.size=0

# Show Tool Calls in Chat
# Enable/disable display of tool call information in the chat panel (default: true)
# When enabled, shows tool name, status, execution time, and result for each tool call
ai.chat.show.tool.calls=true

# Show Thinking Content in Chat
# Enable/disable display of model thinking/reasoning content in the chat panel (default: false)
# When enabled, shows the model's thinking content before tool calls and in the final response
# When disabled, <think> blocks are stripped from output
ai.chat.show.thinking=true

# Tool Result Display Max Length
# Maximum characters to display for tool results (default: 500)
# This setting is shared between:
# - Chat UI display: tool arguments and results in the chat panel
# - Internal logging: tool results stored in ToolEvent for logging
# Content longer than this will be truncated with "..." and total length indicator
ai.chat.tool.result.max.length=500


# API Keys for Search Services
# Brave API Key (optional - https://brave.com/search/api/)
agent.tools.websearch.brave.api.key=

# Tavily API Key (optional - https://tavily.com)
agent.tools.websearch.tavily.api.key=

# Jina API Key (optional - https://jina.ai)
agent.tools.websearch.jina.api.key=


##### LANGSMITH TRACING CONFIGURATION #####

# Enable/disable LangSmith tracing
# LangSmith provides LLM call tracing, monitoring, and evaluation
# Get your API key from https://smith.langchain.com
langsmith.enabled=false

# LangSmith API Key (required for tracing)
# Get your key from https://smith.langchain.com/settings
langsmith.api.key=

# Project Name (default: jmeter-ai)
# All traces will be grouped under this project in LangSmith
langsmith.project.name=jmeter-ai

# LangSmith API Endpoint (optional)
# Default: https://api.smith.langchain.com
# Uncomment to use a custom endpoint (e.g., for EU region or self-hosted)
langsmith.endpoint=https://api.smith.langchain.com

# Sampling rate for tracing (default: 1.0)
# Value between 0.0 and 1.0; 1.0 = trace all requests, 0.5 = trace 50%
langsmith.sample.rate=1.0


##### IPC / CLI CONFIGURATION #####

# Enable the embedded IPC HTTP server so the external CLI (jmeter-cli) can drive
# the running JMeter GUI — add/delete/update elements, or send messages to the Agent.
# Default: true (loopback-only + token auth).
# The CLI discovers the instance via the port file written to
#   <JMETER_HOME>/bin/jmeter-agent/ipc/port-<pid>.json
jmeter.ai.ipc.enabled=true

# Bind address — MUST stay loopback. Default: 127.0.0.1.
# Wildcard values (0.0.0.0, ::, *) are refused for safety.
jmeter.ai.ipc.bind=127.0.0.1

# Listen port. 0 = auto-allocate (recommended; avoids conflicts).
# A fixed port that fails to bind will NOT silently fall back — IPC is skipped.
jmeter.ai.ipc.port=0

# Authentication token. Empty = generated randomly at each GUI start and written
# to the port file (the CLI reads it automatically). Set a fixed value only for
# controlled debugging.
jmeter.ai.ipc.token=

# Sync wait timeout (ms) for the /agent route (the CLI "agent" command).
# Default: 120000 (2 min). Increase for long agent turns.
jmeter.ai.ipc.agent.timeout.ms=120000

# Cross-instance coordination: the list_instances / delegate_to_instance tools let the
# Agent see which .jmx other JMeter instances have open and delegate tasks to them over the
# same IPC channel. These tools are registered automatically when jmeter.ai.ipc.enabled=true
# (no separate switch); with IPC off they are not registered (no transport).
